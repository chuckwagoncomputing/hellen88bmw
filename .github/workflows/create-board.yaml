name: Create Board

on: [ push, pull_request ]

jobs:
  call-workflow-passing-data:
    uses: chuckwagoncomputing/hellen-one/.github/workflows/create-board.yaml@reusable-workflow
    permissions:
      contents: write
